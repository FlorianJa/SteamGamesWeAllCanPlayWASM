# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP .NET Core service.
version: '3.4'

services:
  steamgamesweallcanplay:
    image: steamgamesweallcanplay
    build:
      context: .
      dockerfile: SteamGamesWeAllCanPlayWASM/Server/Dockerfile
    ports:
        - 6080:80
        - 6443:443
  
